# Suppress peer dependency warnings for known compatibility issues
# These are from @keystonehq/sdk expecting older React versions
# but they work fine with React 18.3.1

fund=false
audit-level=moderate

# Suppress specific peer dependency warnings
legacy-peer-deps=false
strict-peer-deps=false